// we render the comment, adding it to comment list when it is created, with ajax.
// we delete the comment, by setting the value of content to null, with ajax


$('#comment-post-<%= @post.id.to_s %>').
  html('<%= j render "posts/comment_list", {post: @post} %>');
$('#comment-form-post-<%= @post.id.to_s %> #comment_content').val("");
console.log("create comment success");